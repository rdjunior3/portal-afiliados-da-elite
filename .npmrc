# Configurações específicas para resolver problemas do Rollup
optional=true
include=optional
fund=false
audit=false
# Força download de dependências nativas específicas da plataforma
force=true
# Evita problemas com cache
cache=false
# Configurações para melhor compatibilidade
install-strategy=hoisted
legacy-peer-deps=true 